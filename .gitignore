dist
.wrangler
.output
.vercel
.netlify
.vinxi
app.config.timestamp_*.js

# Environment
.env
.env*.local

# dependencies
/node_modules

# bun is the only package manager here: `bun install`, `bun run dev`. These
# are listed so a lockfile from another tool can never be committed beside
# `bun.lock` and start disagreeing with it.
package-lock.json
yarn.lock
pnpm-lock.yaml

# IDEs and editors
/.idea
.project
.classpath
*.launch
.settings/

# Local screenshots from `scripts/shoot.mjs`
.shots

# System Files
.DS_Store
Thumbs.db
